import * as THREE from "three";

/*
    
*/

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, 800 / 800, 0, 10);

console.log("scene", scene);

class SceneManager {
  constructior({ canvasWidth, canvasHeignt }) {}
}

